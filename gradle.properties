# Kotlin 2.2 targets at most JVM 24 bytecode while the toolchain is JDK 25 (required by
# keystead-core). Skip the Java/Kotlin JVM-target consistency check; dependency
# resolution stays at JVM 25 and the runtime is JDK 25.
kotlin.jvm.target.validation.mode=IGNORE
